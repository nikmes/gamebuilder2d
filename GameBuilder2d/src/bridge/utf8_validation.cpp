// utf8_validation.cpp (T026)
// Currently all logic is in the header (inline) for simplicity / zero overhead.
// This TU exists to provide a stable place for future extended validation (e.g.,
// normalization, length in code points, etc.).
#include "gb2d/interop/utf8_validation.h"

namespace gb2d::interop::utf8 {
// (intentionally empty)
}
